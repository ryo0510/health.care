<div class="row">
    <div class="col-xs-12 mt-3">
 　    <h4><i class="fas fa-address-book"></i>コース選択</h4>
 　　</div>
   </div>
   <div class="row">
     <div class="col-xs-12">

            <%= form_with(model: @entry, url: entries_path,local: true ) do |f| %>
             <div class="col-xs-10">
               <%= f.label :couse_select, "コース1" %><br>
               <%= f.collection_select(:course_id, Course.all, :id, :name, {:prompt => "--選択してください--"}) %>
             </div>
             <div class="col-xs-10">
               <%= f.label :couse_select, "コース2" %><br>
               <%= f.collection_select(:course_id, Course.all , :id, :name, {:prompt => "--選択してください--"}) %>
             </div>
             <div class="col-xs-10">
               <%= f.label :couse_select, "コース3" %><br>
               <%= f.collection_select(:course_id, Course.all , :id, :name, {:prompt => "--選択してください--"}) %>
             </div>
             <div class="col-xs-10">
               <%= f.label :couse_select, "コース4" %><br>
               <%= f.collection_select(:course_id, Course.all , :id, :name, {:prompt => "--選択してください--"}) %>
             </div>
             <div class="col-xs-10">
               <%= f.label :couse_select, "コース5" %><br>
               <%= f.collection_select(:course_id, Course.all , :id, :name, {:prompt => "--選択してください--"}) %>
             </div>

             <div class="col-xs-8 d-flex justify-content-center mt-5">
               <%= f.submit "保存" , class:"btn btn-primary col-xs-5"  %>
             </div>
             <% end %>


     </div>
   </div>